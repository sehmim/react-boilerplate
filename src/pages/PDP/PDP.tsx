import React, { ReactElement } from 'react'
import Layout from '../../layout'

interface Props {

}

function PDP({ }: Props): ReactElement {
    return (
        <Layout>
            PDP
        </Layout>
    )
}

export default PDP
