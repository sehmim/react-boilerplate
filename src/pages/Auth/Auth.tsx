import React, { ReactElement } from 'react'
import Layout from '../../layout'

interface Props {

}

function Auth({ }: Props): ReactElement {
    return (
        <Layout>
            Auth
        </Layout>
    )
}

export default Auth
