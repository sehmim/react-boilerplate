import React, { ReactElement } from 'react'

interface Props {

}

function Profile({ }: Props): ReactElement {
    return (
        <div>
            Profile
        </div>
    )
}

export default Profile
